{
  "name": "url-shortener-service",
  "version": "1.0.0",
  "description": "A serverless URL shortener service built on AWS Lambda and DynamoDB",
  "main": "src/handlers/createShortUrl.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "url-shortener",
    "aws",
    "lambda",
    "dynamodb",
    "serverless"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "fast-check": "^3.22.0",
    "aws-sdk-client-mock": "^4.1.0"
  }
}
